# Internal working documents - versioned in git, never served.
*.md

# Dead components. Nothing references these; the copies baked into
# support.js are also unreferenced. Kept in git as support.js's source.
SiteHeader.dc.html
InquiryFooter.dc.html

# Cloudflare Pages config. Vercel reads vercel.json instead.
_headers
_redirects
